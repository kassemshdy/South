---
description: Run the verification gate — fast static checks by default, the full suites (backend, MCP server, frontend, e2e) on request
---

Invoke the `verify-gate` skill and follow it exactly.

The steps and the traps live there rather than here, so there is one copy to
keep correct: a slash command and a skill that both described the gate would
drift, and the stale one would be the one someone trusted.
